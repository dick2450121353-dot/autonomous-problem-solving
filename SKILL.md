---
name: autonomous-problem-solving
description: "自主问题处理方法论 — AI Agent 全程自主闭环，不重复错误，经验积累复用。融合 ReAct + Reflexion + Self-Refine + Plan-and-Solve + AutoGPT 精华。"
version: "1.0"
tags: [agent, autonomous, problem-solving, methodology, self-improvement]
author: "Hermes Agent / Nous Research"
---

## Autonomous Problem-Solving / 自主问题处理

用户提出需求后，全程自主闭环，不要中途停下来让用户测试。

### 核心循环（吸收自 ReAct + Reflexion + Plan-and-Solve）

每次行动遵循：**观察 → 思考 → 行动 → 观察结果 → 反思**，形成闭环。

#### 0. 定义成功标准（来自 AutoGPT）
- 动手前先明确：什么情况算"做完了"？列出可验证的条件
- 写进过程文档的"成功标准"字段，防止无限循环

#### 1. 先搜经验
- 处理问题前，先用 `search_files` 搜索经验库（如 `/root/notes/`）下有无相关经验文档
- 搜到就直接复用，跳过已知失败的方法

#### 2. 评估方法（来自 Plan-and-Solve）
- 动手前想清楚：方法对不对？符不符合当前环境？
- **风险评估**：这个方法可能带来什么副作用？（文件覆盖、端口占用、权限问题）
- 之前试过失败的不要再用

#### 3. 写过程文档
- 创建过程文档（如 `PLAN.md`），命名格式：`YYYY-MM-DD-问题关键词.md`
- 内容包含：
  - 背景（用户要什么）
  - **成功标准**（什么情况算完成，可验证的条件）
  - 环境（OS、工具版本、路径等）
  - 用过的方法（每个方法记录：做了什么、结果、**失败根因分析**）
  - 测试结果（验证了什么、数据是什么）
  - 下一步规划（接下来要做什么、为什么这样做）
  - **迭代计数**（当前第几轮，防止无限循环，最多10轮后换个大方向）

#### 4. 读写循环（来自 ReAct）
- 每次行动前读文档确认方向
- 做完后立刻更新文档
- **关键：用文档纠正自己**，不是靠短期记忆

#### 5. 失败反思（来自 Reflexion）
- 每次失败后写清楚：为什么失败？是方法本身错，还是环境不对，还是执行有误？
- 区分"换个方法"和"同一个方法换个参数"
- 记录失败根因，不只是"失败了"

#### 6. 不重复错误
- 文档里记录过的失败方法，绝对不再尝试
- 如果发现又在用同一个方法，立刻停下来换方向

#### 7. 质量验证（来自 Self-Refine）
- 解决方案跑通后，自检：是否满足第0步的成功标准？
- 有没有遗漏的边界情况？
- 代码/文件是否干净？（多余文件要清理）

#### 8. 总结沉淀
- 问题解决后在文档末尾写总结：
  - 什么方法有效，什么方法无效
  - 关键陷阱和避坑指南
  - 可复用的代码片段或命令模板
- 方便下次复用

### 文档命名规则

- 格式：`YYYY-MM-DD-简短描述.md`
- 存放：经验库目录（如 `/root/notes/`）
- 示例：`2026-05-11-关闭-openclaw-bat.md`
- 下次遇到类似问题，先搜这个目录找经验

### 适配指南

#### 对 AI Agent（如 Hermes Agent）
- 经验库路径：`/root/notes/`
- 过程文档：`PLAN.md` 或独立文件
- 使用 `search_files` 搜索经验
- 多个Agent可共享同一经验库

#### 对 OpenClaw
- 经验库：workspace目录下的文件
- 过程文档：`PLAN.md`
- 搜workspace下有无相关文档

#### 对人类用户
- 不需要写那么复杂，备忘录即可：问题→试了什么→结果→结论
- 关键是"不重复错误"——记录了要回头看
- 推荐 Obsidian 等双向链接工具，经验之间可关联

### 适用场景

1. **AI Agent** — 解决"记不住"和"重复犯错"的问题
2. **独立开发者** — 一个人干活需要自己计划、验证、复盘
3. **运维/SRE** — 排障天然就是这个循环
4. **学习新技术** — 踩坑记录比刷教程快

### 方法论来源

本流程融合了以下框架的精华：
- **Chain of Thought** — 逐步推理，不想当然
- **ReAct** — 观察→思考→行动→观察的循环
- **Reflexion** — 失败后结构化反思，分析根因
- **Self-Refine** — 完成后自检质量，不是跑通就算完
- **Plan-and-Solve** — 先规划再执行，评估风险
- **AutoGPT** — 定义成功标准，防止无限循环
- **文件驱动** — 用文档而非短期记忆来驱动循环，跨session可复用
