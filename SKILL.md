---
name: autonomous-problem-solving
description: "资源感知型自主执行引擎 — 文本驱动记忆闭环 + 动态资源发现 + 主动研究。不确定时先搜再做。"
version: "2.0"
tags: [agent, autonomous, memory-loop, resource-aware, research-driven]
author: "Hermes Agent / Nous Research"
---

## 核心：文本驱动记忆闭环

所有任务围绕**文档**运转。文档是记忆，不是装饰。

```
搜经验 → 写PLAN → 执行 → 更新文档 → 反思 → 沉淀
  ↑                                          ↓
  └────────── 下次任务自动检索 ←──────────────┘
```

每个非简单任务按此流程执行（步骤0-8）：

### 步骤 0：预判复杂度

任务进来先判断，决定搜索深度：

- **简单**（单文件/单命令/方向明确）→ 跳到步骤3直接执行
- **中等**（多文件/方向明确但有依赖）→ 步骤1本地 + 步骤2匹配skill
- **复杂**（不确定/多组件/没见过的问题）→ 全流程：搜索 → 研究 → 执行

### 步骤 1：搜经验（本地）

```
1. search_files("关键词", path="/root/notes/")   ← 经验库
2. session_search("关键词")                       ← 历史会话
3. search_files("关键词", path="项目根目录")       ← 项目源码
```

搜到经验 → 读取，跳过已知失败方法，复用成功路径。

### 步骤 2：搜资源（扩展）

中等以上任务，继续搜索可用资源：

```
4. skills_list() → 匹配相关skill → skill_view() 加载
5. tavily-search / web_search("问题关键词")       ← 网上方案
6. GitHub API 搜索相关repo/代码/issue             ← 开源参考
```

**资源评估**：不全有用，按相关性、质量、成本排序，选Top N。

**搜索不够？** 换关键词、换渠道继续搜。不要猜，要查。

### 步骤 3：写 PLAN.md

每个非简单任务必须写。五项必填：

```markdown
# PLAN: [任务名]

## 背景
用户要什么，一句话。

## 成功标准
什么情况算"做完了"。可验证的条件，列清楚。

## 可用资源
- 经验：[从步骤1搜到的]
- Skills：[从步骤2匹配的]
- 参考：[网上/GitHub找到的方案]

## 方法
用什么技术方案，为什么选这个。

## 方案（含用时）
1. [ ] 步骤一 — 预估: Xmin — 实际: __min
2. [ ] 步骤二 — 预估: Xmin — 实际: __min
3. [ ] 步骤三 — 预估: Xmin — 实际: __min
   总预估: __min — 总实际: __min

## 迭代计数
当前第 __ 轮（最多10轮后换大方向）

## 进度/反馈
（执行过程中更新）
```

### 步骤 4：执行

默认：PLAN写完直接执行。仅当用户主动要求先看PLAN时才等待。

核心：用文档纠正自己，不是靠短期记忆。

每步执行遵循：
```
读PLAN确认方向 → ACT → VERIFY → 更新PLAN进度
```

- 每步完成后立刻更新 PLAN.md 的进度
- 长时间操作每分钟反馈：当前步骤、做了什么、结果、下一步
- 验证失败 → 记录失败现象，进入步骤5

### 步骤 5：失败处理

**反思（Reflexion）**：失败后写清楚：

- **现象**：具体错误信息/输出
- **根因**：方法本身错？环境不对？执行有误？
- **区分**：换个方法 vs 同一方法换参数

**升级（10轮分级）**：
```
轮 1-3：同方法微调参数
轮 4-6：换完全不同的方法
轮 7-9：换技术栈/框架/工具
轮 10：停止，写经验文档，报告用户
```

**铁律**：文档里记录过的失败方法，绝对不再尝试。发现又在用同一个方法，立刻停下来。

### 步骤 6：验证门控

**铁律：没有验证证据禁止声称完成。**

验证前的自问：
1. 什么命令/操作能证明成功？
2. 我运行了吗？看到完整输出了吗？
3. 输出确实证明成功了吗？（不是"应该成功了"）

验证类型：
- 代码：运行命令 + 检查输出 + exit code
- 文件：stat + read back 内容
- 网络：fetch URL + 检查状态码
- 进程：检查存活 + 测试完必须杀掉
- 外部操作：必须验证副作用（HTTP状态码、文件存在）

验证后报告：带证据（命令输出/文件内容/状态码），不说"应该成功了"。

禁止词：应该、可能、大概、probably、should work

### 步骤 7：经验沉淀

任务结束，写入 `/root/notes/YYYY-MM-DD-关键词.md`：

```markdown
## 总结
- 什么方法有效
- 什么方法无效（附根因）
- 关键陷阱和避坑指南
- 可复用的代码/命令模板
- 发现的新资源（skill/工具/参考）
```

复杂任务成功后，考虑保存为 skill（`skill_manage`）。

## 工具映射

| 步骤 | 工具 | 用途 |
|------|------|------|
| 搜经验 | `search_files` | 搜索本地经验库和项目源码 |
| 搜会话 | `session_search` | 搜索历史对话 |
| 搜skill | `skills_list` / `skill_view` | 发现和加载skill |
| 搜网络 | `tavily-search` / `web_search` | 搜索网上方案 |
| 搜代码 | GitHub API | 搜索开源参考 |
| 写PLAN | `write_file` | 创建过程文档 |
| 更新PLAN | `patch` | 增量更新文档 |
| 执行 | `terminal` / `execute_code` | 运行命令和脚本 |
| 任务管理 | `todo` | checklist跟踪 |
| 并行 | `delegate_task` | 多个独立子任务并行 |
| 沉淀 | `write_file` / `skill_manage` | 保存经验/创建skill |
